apiVersion: v1
kind: Service
metadata:
  name: {{ include "3ridge.fullname" . }}
  labels:
    {{- include "3ridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "3ridge.selectorLabels" . | nindent 4 }}
